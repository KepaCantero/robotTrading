# Requirements Document: infrastructure/logging/append_only_log.py

## 1. Module Overview
Append-Only Log Storage

Almacenamiento append-only para logs de trading.

## 2. Dependencies
- `datetime.date`
- `datetime.datetime`
- `datetime.timedelta`
- `pathlib.Path`
- `json`
- `typing.List`
- `app.infrastructure.logging.log_entry.LogEntry`

## 3. Core Components (Contracts)
### Public Classes
- `AppendOnlyLog`: Must conform to application domain models.
### Public Functions
- `append()`: Must return strictly typed objects.
- `get_entries_by_correlation_id()`: Must return strictly typed objects.
- `export_date_range()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [ ] Exceptions must be handled.
