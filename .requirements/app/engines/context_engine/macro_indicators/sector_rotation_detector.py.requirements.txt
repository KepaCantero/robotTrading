# Requirements Document: engines/context_engine/macro_indicators/sector_rotation_detector.py

## 1. Module Overview
SectorRotationDetector - Detector de rotación sectorial (simplificado).

## 2. Dependencies
- `logging`
- `typing.Any`
- `typing.Dict`
- `typing.List`
- `numpy`

## 3. Core Components (Contracts)
### Public Classes
- `SectorRotationDetector`: Must conform to application domain models.
### Public Functions
- `detect_rotation()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [ ] Exceptions must be handled.
