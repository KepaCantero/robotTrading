# Requirements Document: engines/risk_engine/risk_limits_enforcer.py

## 1. Module Overview
Risk Limits Enforcer - Hull Chapter 18

Implements automatic risk limit enforcement based on VaR thresholds.

Enforcement actions:
1. Position reduction when VaR exceeds limits
2. Trading halt when VaR breaches critical threshold
3. Dynamic position sizing based on VaR utilization
4. Portfolio heatmaps by risk factor
5. Risk attribution by asset class

This module implements the risk management controls recommended in Hull:
- Stop-loss limits
- VaR-based position limits
- Concentration limits
- Leverage limits

Reference: Hull, Options, Futures, and Other Derivatives, Chapter 18

## 2. Dependencies
- `logging`
- `datetime.datetime`
- `decimal.Decimal`
- `typing.Any`
- `typing.Dict`
- `typing.List`
- `typing.Optional`
- `numpy`
- `app.shared.config.centralized_config.get_config`
- `app.domain.models.portfolio.Portfolio`

## 3. Core Components (Contracts)
### Public Classes
- `RiskLimitsEnforcer`: Must conform to application domain models.
### Public Functions
- `check_var_limits()`: Must return strictly typed objects.
- `enforce_position_limits()`: Must return strictly typed objects.
- `generate_risk_heatmap()`: Must return strictly typed objects.
- `attribute_risk_by_asset_class()`: Must return strictly typed objects.
- `calculate_dynamic_position_size()`: Must return strictly typed objects.
- `reset_trading_halt()`: Must return strictly typed objects.
- `get_enforcement_status()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [x] Exceptions must be handled.
