# Requirements Document: engines/strategy_engines/base.py

## 1. Module Overview
BaseStrategyEngine - Clase base abstracta para todos los strategy engines.

Extiende BaseStrategy con funcionalidades adicionales para:
- Integración con Learning Engines
- Feature extraction estandarizado
- Callbacks para aprendizaje continuo
- Ajustes dinámicos basados en predicciones
- Soporte para composición de estrategias (ensembles)
- Integración con DataEngine y ContextEngine (Módulos 1 y 2)

## 2. Dependencies
- `asyncio`
- `logging`
- `abc.ABC`
- `abc.abstractmethod`
- `collections.abc.Sequence`
- `datetime.datetime`
- `decimal.Decimal`
- `typing.Any`
- `typing.Callable`
- `typing.Dict`
- `typing.List`
- `typing.Optional`
- `app.domain.models.market_data.Quote`
- `app.domain.models.signal.Signal`
- `app.domain.strategies.base.BaseStrategy`
- `app.engines.data_engine.data_engine.DataEngine`
- `app.engines.context_engine.context_engine.ContextEngine`
- `app.engines.portfolio_engine.portfolio_engine.PortfolioEngine`
- `app.engines.risk_engine.risk_engine.RiskEngine`

## 3. Core Components (Contracts)
### Public Classes
- `BaseStrategyEngine`: Must conform to application domain models.
### Public Functions
- `extract_features()`: Must return strictly typed objects.
- `get_strategy_type()`: Must return strictly typed objects.
- `set_learning_engine()`: Must return strictly typed objects.
- `get_learning_prediction()`: Must return strictly typed objects.
- `apply_learning_adjustments()`: Must return strictly typed objects.
- `register_signal_callback()`: Must return strictly typed objects.
- `register_trade_callback()`: Must return strictly typed objects.
- `register_market_data_callback()`: Must return strictly typed objects.
- `set_ensemble_weight()`: Must return strictly typed objects.
- `get_ensemble_weight()`: Must return strictly typed objects.
- `get_context_analysis()`: Must return strictly typed objects.
- `get_volatility_regime()`: Must return strictly typed objects.
- `get_market_data_from_engine()`: Must return strictly typed objects.
- `set_data_engine()`: Must return strictly typed objects.
- `set_context_engine()`: Must return strictly typed objects.
- `set_portfolio_engine()`: Must return strictly typed objects.
- `set_risk_engine()`: Must return strictly typed objects.
- `generate_signals()`: Must return strictly typed objects.
- `get_metrics()`: Must return strictly typed objects.
- `reset_metrics()`: Must return strictly typed objects.
- `get_status()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [x] Exceptions must be handled.
