# Requirements Document: domain/strategies/modules/market_detectors/base_detector.py

## 1. Module Overview
BaseMarketDetector - Clase base abstracta para detectores de régimen de mercado.

## 2. Dependencies
- `logging`
- `abc.ABC`
- `abc.abstractmethod`
- `typing.Dict`
- `typing.List`
- `typing.Optional`
- `app.shared.config.config_loader.get_detector_config`

## 3. Core Components (Contracts)
### Public Classes
- `BaseMarketDetector`: Must conform to application domain models.
### Public Functions
- `detect()`: Must return strictly typed objects.
- `is_enabled()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [x] Exceptions must be handled.
