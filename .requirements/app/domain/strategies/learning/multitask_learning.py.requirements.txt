# Requirements Document: domain/strategies/learning/multitask_learning.py

## 1. Module Overview
MultiTaskLearning - Sistema de aprendizaje multi-tarea para trading.

Incluye:
1. Compartir representaciones entre tareas (return, Sharpe, drawdown)
2. Multi-objective optimization con pesos adaptativos
3. Task-specific heads con shared backbone

## 2. Dependencies
- `logging`
- `collections.defaultdict`
- `typing.Any`
- `typing.Dict`
- `typing.List`
- `typing.Optional`
- `typing.Tuple`
- `numpy`
- `pandas`
- `torch`
- `torch.nn`
- `torch.optim`

## 3. Core Components (Contracts)
### Public Classes
- `SharedBackbone`: Must conform to application domain models.
- `TaskHead`: Must conform to application domain models.
- `MultiTaskModel`: Must conform to application domain models.
- `MultiObjectiveOptimizer`: Must conform to application domain models.
- `MultiTaskLearningEngine`: Must conform to application domain models.
### Public Functions
- `forward()`: Must return strictly typed objects.
- `forward()`: Must return strictly typed objects.
- `forward()`: Must return strictly typed objects.
- `compute_weighted_loss()`: Must return strictly typed objects.
- `update_weights_adaptive()`: Must return strictly typed objects.
- `get_objective_weights()`: Must return strictly typed objects.
- `train()`: Must return strictly typed objects.
- `predict()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [x] Exceptions must be handled.
