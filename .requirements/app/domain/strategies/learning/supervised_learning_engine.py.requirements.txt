# Requirements Document: domain/strategies/learning/supervised_learning_engine.py

## 1. Module Overview
SupervisedLearningEngine - Aprende a predecir probabilidad de éxito de trades.

Implements López de Prado's sample weights by uniqueness to account for
overlapping samples in financial ML training (Chapter 4, "Advances in Financial Machine Learning").

## 2. Dependencies
- `logging`
- `typing.Any`
- `typing.Dict`
- `typing.List`
- `typing.Optional`
- `numpy`
- `pandas`
- `base_learning_engine.BaseLearningEngine`
- `catboost`
- `lightgbm`
- `torch`
- `torch.nn`
- `xgboost`
- `sklearn.ensemble.GradientBoostingClassifier`
- `sklearn.ensemble.RandomForestClassifier`
- `sklearn.metrics.accuracy_score`
- `sklearn.metrics.f1_score`
- `sklearn.metrics.precision_score`
- `sklearn.metrics.recall_score`
- `sklearn.metrics.roc_auc_score`
- `sklearn.model_selection.train_test_split`
- `app.domain.strategies.momentum_modular.learning.feature_extractor.FeatureExtractor`
- `app.backtesting.metrics.calculate_matthews_corrcoef`
- `app.backtesting.validation.cross_validation.PurgedKFold`
- `app.backtesting.labeling.triple_barrier.calculate_sample_weights_uniqueness`

## 3. Core Components (Contracts)
### Public Classes
- `SupervisedLearningEngine`: Must conform to application domain models.
- `LightGBMWrapper`: Must conform to application domain models.
- `WeightedBCELoss`: Must conform to application domain models.
### Public Functions
- `train()`: Must return strictly typed objects.
- `predict()`: Must return strictly typed objects.
- `evaluate()`: Must return strictly typed objects.
- `predict_proba()`: Must return strictly typed objects.
- `predict()`: Must return strictly typed objects.
- `feature_importances_()`: Must return strictly typed objects.
- `forward()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [x] Exceptions must be handled.
