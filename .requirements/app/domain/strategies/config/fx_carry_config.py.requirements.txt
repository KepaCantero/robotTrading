# Requirements Document: domain/strategies/config/fx_carry_config.py

## 1. Module Overview
FX Carry Trade Strategy Configuration

This module contains all configuration parameters for the FX carry trade strategy.
Centralized from fx_carry_trade/fx_carry_trade_strategy.py hardcoded values.

## 2. Dependencies
- `pydantic.BaseModel`
- `pydantic.Field`

## 3. Core Components (Contracts)
### Public Classes
- `FXCarryTradeStrategyConfig`: Must conform to application domain models.

## 4. Execution Requirements (Compliance Rules)
- [ ] Must execute without syntax errors.
- [ ] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [ ] Exceptions must be handled.
