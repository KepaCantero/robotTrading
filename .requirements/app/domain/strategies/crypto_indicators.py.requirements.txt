# Requirements Document: domain/strategies/crypto_indicators.py

## 1. Module Overview
Crypto-Specific Technical Indicators

This module provides technical indicators optimized for cryptocurrency markets,
including on-chain metrics, network health indicators, and sentiment measures.

Crypto-specific indicators differ from traditional markets:
- Network Value to Transactions (NVT) ratio
- Mayer Multiple (for Bitcoin)
- Active addresses and transaction counts
- Hash rate (for PoW coins)
- Staking ratios (for PoS coins)
- Token velocity
- Fear & Greed index components

## 2. Dependencies
- `logging`
- `decimal.Decimal`
- `typing.Dict`
- `typing.Optional`
- `typing.Tuple`
- `numpy`
- `pandas`
- `models.OnChainMetrics`

## 3. Core Components (Contracts)
### Public Classes
- `CryptoIndicators`: Must conform to application domain models.
### Public Functions
- `calculate_nvt_ratio()`: Must return strictly typed objects.
- `calculate_mayer_multiple()`: Must return strictly typed objects.
- `calculate_fear_greed_index()`: Must return strictly typed objects.
- `calculate_relative_strength()`: Must return strictly typed objects.
- `calculate_momentum_score()`: Must return strictly typed objects.
- `calculate_crypto_beta()`: Must return strictly typed objects.
- `calculate_network_health_score()`: Must return strictly typed objects.
- `calculate_token_velocity()`: Must return strictly typed objects.
- `calculate_staking_apr()`: Must return strictly typed objects.
- `calculate_rsi()`: Must return strictly typed objects.
- `calculate_ema()`: Must return strictly typed objects.
- `calculate_macd()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [ ] Exceptions must be handled.
