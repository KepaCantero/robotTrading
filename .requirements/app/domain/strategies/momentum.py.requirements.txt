# Requirements Document: domain/strategies/momentum.py

## 1. Module Overview
MomentumStrategy - Estrategia de momentum basada en RSI, EMA y volumen.

REFACTORED: Usa indicadores vectorizados de TechnicalIndicatorCalculator
y corrige señales invertidas (BUY con RSI alto, SELL con RSI bajo).

Implementa una estrategia de momentum que utiliza indicadores técnicos
para identificar oportunidades de trading basadas en tendencias de precio.

## 2. Dependencies
- `__future__.annotations`
- `logging`
- `collections.deque`
- `decimal.Decimal`
- `typing.Any`
- `app.shared.config.centralized_config.get_strategy_config`
- `app.shared.config.centralized_config.get_trading_threshold`
- `app.shared.config.centralized_config.get_config`
- `app.domain.models.market_data.Quote`
- `app.domain.models.portfolio.Portfolio`
- `app.domain.models.signal.Signal`
- `app.domain.models.signal.SignalSource`
- `app.domain.models.signal.SignalStrength`
- `app.domain.models.signal.SignalType`
- `app.domain.services.analysis.momentum.TechnicalIndicatorCalculator`
- `app.services.signal_scoring_engine.get_signal_scoring_engine`
- `base.BaseStrategy`

## 3. Core Components (Contracts)
### Public Classes
- `MomentumStrategy`: Must conform to application domain models.
### Public Functions
- `get_required_parameters()`: Must return strictly typed objects.
- `generate_signals()`: Must return strictly typed objects.
- `risk_check()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [x] Exceptions must be handled.
