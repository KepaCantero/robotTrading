# Requirements Document: domain/strategies/strategy_logger.py

## 1. Module Overview
StrategyLogger - Logger centralizado para estrategias.

Proporciona logging estructurado y métricas para todas las estrategias,
incluyendo señales generadas, ejecutadas, rechazadas y errores.

## 2. Dependencies
- `json`
- `logging`
- `datetime.datetime`
- `decimal.Decimal`
- `pathlib.Path`
- `typing.Any`
- `typing.Dict`
- `typing.List`
- `typing.Optional`
- `app.domain.models.signal.Signal`

## 3. Core Components (Contracts)
### Public Classes
- `StrategyLogger`: Must conform to application domain models.
### Public Functions
- `log_signal_generated()`: Must return strictly typed objects.
- `log_signal_rejected()`: Must return strictly typed objects.
- `log_signal_executed()`: Must return strictly typed objects.
- `log_strategy_error()`: Must return strictly typed objects.
- `log_execution_error()`: Must return strictly typed objects.
- `log_strategy_loaded()`: Must return strictly typed objects.
- `log_strategy_unloaded()`: Must return strictly typed objects.
- `log_strategy_activated()`: Must return strictly typed objects.
- `log_strategy_deactivated()`: Must return strictly typed objects.
- `get_strategy_metrics()`: Must return strictly typed objects.
- `get_all_metrics()`: Must return strictly typed objects.
- `clear_logs()`: Must return strictly typed objects.
- `export_logs()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [x] Exceptions must be handled.
