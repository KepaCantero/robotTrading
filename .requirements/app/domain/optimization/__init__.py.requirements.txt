# Requirements Document: domain/optimization/__init__.py

## 1. Module Overview
Optimization Module - Unified Interface.

This module provides a unified interface for all optimization algorithms
in the trading system, including:

1. Parameter Optimizers:
   - BayesianOptimizer: Bayesian optimization using Optuna (TPE sampler)
   - GridSearchOptimizer: Exhaustive grid search
   - GridSearchOptimizerCV: Grid search with cross-validation

2. Portfolio Optimizers:
   - MeanVarianceOptimizer: Markowitz mean-variance optimization

3. Factory:
   - OptimizerFactory: Creates optimizers by type

Quick Start:
    ```python
    from app.domain.optimization import (
        OptimizerFactory,
        OptimizationConfig,
        BayesianOptimizer,
        GridSearchOptimizer,
        MeanVarianceOptimizer,
        SearchSpace,
    )

    # Create using factory
    optimizer = OptimizerFactory.create("bayesian", n_trials=100)

    # Or create directly
    config = OptimizationConfig(maximize=True, metric="sharpe_ratio")
    optimizer = BayesianOptimizer(config, n_trials=100)

    # Define search space
    space = SearchSpace()
    space.add_integer("lookback", 5, 50, step=5)
    space.add_continuous("threshold", 0.01, 0.1, log=True)

    # Run optimization
    result = await optimizer.optimize(objective_func, space)
    print(f"Best params: {result.best_params}")
    print(f"Best score: {result.best_score}")
    ```

Portfolio Optimization:
    ```python
    from app.domain.optimization import MeanVarianceOptimizer

    optimizer = MeanVarianceOptimizer(
        risk_free_rate=0.02,  # or use CentralizedConfig.backtesting.default_risk_free_rate
        max_position=0.20,
    )

    # Optimize for max Sharpe
    result = optimizer.optimize_portfolio(returns, method="max_sharpe")
    print(f"Optimal weights: {result.weights_dict}")
    print(f"Expected Sharpe: {result.sharpe_ratio:.2f}")

    # Get efficient frontier
    frontier = optimizer.compute_efficient_frontier(returns)
    ```

## 2. Dependencies
- `base_optimizer.BaseOptimizer`
- `base_optimizer.OptimizationConfig`
- `base_optimizer.OptimizationResult`
- `base_optimizer.OptimizationStatus`
- `base_optimizer.OptimizerType`
- `base_optimizer.TrialResult`
- `bayesian_optimizer.BayesianOptimizer`
- `bayesian_optimizer.MultiObjectiveBayesianOptimizer`
- `bayesian_optimizer.SearchSpace`
- `grid_search_optimizer.GridSearchOptimizer`
- `grid_search_optimizer.GridSearchOptimizerCV`
- `mean_variance_optimizer.MeanVarianceOptimizer`
- `mean_variance_optimizer.EfficientFrontier`
- `mean_variance_optimizer.EfficientFrontierPoint`
- `mean_variance_optimizer.PortfolioOptimizationResult`
- `mean_variance_optimizer.OptimizationMethod`
- `mean_variance_optimizer.ShrinkageMethod`
- `mean_variance_optimizer.InputValidationError`
- `mean_variance_optimizer.OptimizationError`
- `factory.OptimizerFactory`
- `factory.create_optimizer`
- `factory.create_portfolio_optimizer`
- `factory.create_backtest_optimizer`

## 3. Core Components (Contracts)

## 4. Execution Requirements (Compliance Rules)
- [ ] Must execute without syntax errors.
- [ ] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [ ] Exceptions must be handled.
