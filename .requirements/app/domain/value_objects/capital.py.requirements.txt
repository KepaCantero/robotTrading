# Requirements Document: domain/value_objects/capital.py

## 1. Module Overview
Capital Value Object - Trading capital allocation

Capital represents the allocated trading capital with associated
tier and risk constraints.

## 2. Dependencies
- `__future__.annotations`
- `dataclasses.dataclass`
- `decimal.Decimal`
- `enum.Enum`
- `typing.Any`
- `money.Money`

## 3. Core Components (Contracts)
### Public Classes
- `CapitalTier`: Must conform to application domain models.
- `Capital`: Must conform to application domain models.
### Public Functions
- `get_tier()`: Must return strictly typed objects.
- `get_amount()`: Must return strictly typed objects.
- `get_max_exposure()`: Must return strictly typed objects.
- `can_add_position()`: Must return strictly typed objects.
- `is_strategy_enabled()`: Must return strictly typed objects.
- `from_amount()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [ ] Must execute without syntax errors.
- [ ] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [x] Exceptions must be handled.
