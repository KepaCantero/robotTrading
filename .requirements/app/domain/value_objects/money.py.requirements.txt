# Requirements Document: domain/value_objects/money.py

## 1. Module Overview
Money Value Object - Immutable monetary value

Money is a value object representing a monetary amount with currency.
It is immutable and defined by its attributes.

## 2. Dependencies
- `__future__.annotations`
- `dataclasses.dataclass`
- `decimal.Decimal`
- `typing.Any`

## 3. Core Components (Contracts)
### Public Classes
- `Money`: Must conform to application domain models.
### Public Functions
- `is_zero()`: Must return strictly typed objects.
- `is_positive()`: Must return strictly typed objects.
- `to_float()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [ ] Must execute without syntax errors.
- [ ] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [x] Exceptions must be handled.
