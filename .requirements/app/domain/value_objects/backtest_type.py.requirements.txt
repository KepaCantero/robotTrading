# Requirements Document: domain/value_objects/backtest_type.py

## 1. Module Overview
BacktestType Enum - Types of backtesting operations

Defines the different types of backtests supported by the system.

## 2. Dependencies
- `enum.Enum`

## 3. Core Components (Contracts)
### Public Classes
- `BacktestType`: Must conform to application domain models.

## 4. Execution Requirements (Compliance Rules)
- [ ] Must execute without syntax errors.
- [ ] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [ ] Exceptions must be handled.
