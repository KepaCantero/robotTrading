# Requirements Document: domain/value_objects/backtest_result.py

## 1. Module Overview
BacktestResult Value Object - Results from backtesting operations

This value object encapsulates all results from a backtest execution.
It's immutable and contains performance metrics and statistics.

## 2. Dependencies
- `__future__.annotations`
- `dataclasses.dataclass`
- `dataclasses.field`
- `decimal.Decimal`
- `typing.Any`
- `typing.Dict`
- `typing.Optional`

## 3. Core Components (Contracts)
### Public Classes
- `BacktestResultValue`: Must conform to application domain models.
### Public Functions
- `roi()`: Must return strictly typed objects.
- `is_profitable()`: Must return strictly typed objects.
- `has_acceptable_drawdown()`: Must return strictly typed objects.
- `to_dict()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [ ] Must execute without syntax errors.
- [ ] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [x] Exceptions must be handled.
