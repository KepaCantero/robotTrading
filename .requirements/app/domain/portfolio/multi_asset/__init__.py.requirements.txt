# Requirements Document: domain/portfolio/multi_asset/__init__.py

## 1. Module Overview
Multi-Asset Portfolio Support Module

This module provides comprehensive support for managing portfolios across multiple asset classes
including equities, fixed income, crypto, forex, commodities, real estate, and cash.

FASE 7.2 - Multi-Asset Portfolio Support

Key Features:
- Asset class definition and management
- Multi-asset portfolio construction
- Strategic and tactical allocation
- Risk parity allocation
- Multi-asset rebalancing
- Risk contribution analysis
- Correlation-aware allocation

Example Usage:
    >>> from app.domain.portfolio.multi_asset import (
    ...     MultiAssetPortfolioManager,
    ...     MultiAssetConfig,
    ...     AssetClass,
    ...     AssetClassType
    ... )
    >>>
    >>> # Define asset classes
    >>> equity_class = AssetClass(
    ...     name="US Equities",
    ...     type=AssetClassType.EQUITY,
    ...     expected_return=Decimal("0.08"),
    ...     volatility=Decimal("0.15")
    ... )
    >>>
    >>> # Create portfolio manager
    >>> config = MultiAssetConfig(
    ...     asset_classes=[equity_class, ...],
    ...     rebalance_threshold=Decimal("0.05")
    ... )
    >>> manager = MultiAssetPortfolioManager(config)
    >>>
    >>> # Construct portfolio
    >>> portfolio = manager.construct_portfolio(
    ...     target_weights={"equity": Decimal("0.6"), "bonds": Decimal("0.4")},
    ...     market_data=market_data
    ... )

## 2. Dependencies
- `allocation.AllocationResult`
- `allocation.MarketRegime`
- `allocation.MultiAssetAllocator`
- `allocation.RiskParityAllocationParams`
- `allocation.StrategicAllocationParams`
- `allocation.TacticalAllocationParams`
- `asset_class.AssetClass`
- `asset_class.AssetClassConfig`
- `asset_class.AssetClassMetrics`
- `asset_class.AssetClassReturns`
- `asset_class.AssetClassType`
- `asset_class.RebalanceFrequency`
- `models.AllocationStrategy`
- `models.MultiAssetAllocation`
- `models.MultiAssetPortfolio`
- `models.PortfolioMetrics`
- `models.RiskTolerance`
- `app.backtesting.models.Trade`
- `multi_asset_portfolio.MultiAssetConfig`
- `multi_asset_portfolio.MultiAssetPortfolioManager`
- `multi_asset_portfolio.PortfolioConstructionResult`
- `multi_asset_portfolio.RebalanceResult`
- `rebalancer.CostEstimate`
- `rebalancer.MultiAssetRebalancer`
- `rebalancer.RebalancePlan`
- `rebalancer.RebalancePriority`
- `rebalancer.RebalanceTrade`

## 3. Core Components (Contracts)

## 4. Execution Requirements (Compliance Rules)
- [ ] Must execute without syntax errors.
- [ ] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [ ] Exceptions must be handled.
