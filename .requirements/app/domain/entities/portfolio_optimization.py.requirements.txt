# Requirements Document: domain/entities/portfolio_optimization.py

## 1. Module Overview
Portfolio Optimization Entity - Domain Layer

This entity represents a portfolio optimization result.
It's a pure domain entity with no infrastructure dependencies.

## 2. Dependencies
- `__future__.annotations`
- `dataclasses.dataclass`
- `dataclasses.field`
- `decimal.Decimal`
- `typing.Any`

## 3. Core Components (Contracts)
### Public Classes
- `PortfolioOptimization`: Must conform to application domain models.
### Public Functions
- `get_weight_summary()`: Must return strictly typed objects.
- `get_top_positions()`: Must return strictly typed objects.
- `get_risk_metrics()`: Must return strictly typed objects.
- `get_regime_info()`: Must return strictly typed objects.
- `is_efficient()`: Must return strictly typed objects.
- `is_diversified()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [ ] Must execute without syntax errors.
- [ ] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [x] Exceptions must be handled.
