# Requirements Document: domain/models/tax_config.py

## 1. Module Overview
Tax Configuration model.

Optimizes tax treatment based on investor's tax residence.
References López de Prado's tax optimization for asset managers.

Reference:
- López de Prado: Machine Learning for Asset Managers (46-lopez-de-prado-machine-learning-asset-managers.md)

## 2. Dependencies
- `decimal.Decimal`
- `typing.Optional`
- `pydantic.BaseModel`
- `pydantic.ConfigDict`
- `pydantic.Field`

## 3. Core Components (Contracts)
### Public Classes
- `TaxConfig`: Must conform to application domain models.
### Public Functions
- `has_long_term_advantage()`: Must return strictly typed objects.
- `long_term_advantage()`: Must return strictly typed objects.
- `is_tax_friendly()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [ ] Must execute without syntax errors.
- [ ] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [ ] Exceptions must be handled.
