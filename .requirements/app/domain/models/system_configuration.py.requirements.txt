# Requirements Document: domain/models/system_configuration.py

## 1. Module Overview
System Configuration model.

Complete configuration derived from InputProfile.
Combines strategy selection, risk parameters, and tax optimization.

This is the output of InputProfileRouter and serves as the complete
configuration for the trading system.

## 2. Dependencies
- `decimal.Decimal`
- `typing.Optional`
- `pydantic.BaseModel`
- `pydantic.ConfigDict`
- `pydantic.Field`
- `app.domain.models.risk_config.RiskConfig`
- `app.domain.models.strategy_type.StrategyType`
- `app.domain.models.tax_config.TaxConfig`

## 3. Core Components (Contracts)
### Public Classes
- `SystemConfiguration`: Must conform to application domain models.
### Public Functions
- `deployable_capital()`: Must return strictly typed objects.
- `requires_long_term_focus()`: Must return strictly typed objects.
- `is_complex_strategy()`: Must return strictly typed objects.
- `expected_volatility()`: Must return strictly typed objects.
- `to_dict()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [ ] Exceptions must be handled.
