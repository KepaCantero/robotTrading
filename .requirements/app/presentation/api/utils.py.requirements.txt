# Requirements Document: presentation/api/utils.py

## 1. Module Overview
API utility functions for common patterns.

This module provides utility functions for correlation IDs, rate limiting,
timeout handling, and structured logging in API endpoints.

## 2. Dependencies
- `asyncio`
- `logging`
- `time`
- `uuid`
- `contextlib.asynccontextmanager`
- `functools.wraps`
- `typing.Any`
- `typing.AsyncGenerator`
- `typing.Callable`
- `typing.Dict`
- `typing.Optional`
- `typing.TypeVar`
- `fastapi.HTTPException`
- `fastapi.Request`
- `fastapi.Response`
- `fastapi.status`
- `starlette.middleware.base.BaseHTTPMiddleware`
- `app.infrastructure.logging.logging_config.get_correlation_id`
- `app.infrastructure.logging.logging_config.set_correlation_id`

## 3. Core Components (Contracts)
### Public Classes
- `RateLimiter`: Must conform to application domain models.
- `CorrelationIdMiddleware`: Must conform to application domain models.
### Public Functions
- `get_rate_limiter()`: Must return strictly typed objects.
- `with_timeout()`: Must return strictly typed objects.
- `with_rate_limit()`: Must return strictly typed objects.
- `log_endpoint_call()`: Must return strictly typed objects.
- `log_error_with_trace()`: Must return strictly typed objects.
- `is_allowed()`: Must return strictly typed objects.
- `reset()`: Must return strictly typed objects.
- `decorator()`: Must return strictly typed objects.
- `decorator()`: Must return strictly typed objects.
- `decorator()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [x] Exceptions must be handled.
