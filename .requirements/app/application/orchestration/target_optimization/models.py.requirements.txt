# Requirements Document: application/orchestration/target_optimization/models.py

## 1. Module Overview
PHASE 1: Capital-Tier Aware Strategy Orchestration - Data Models

Defines:
- CapitalTier enum and tier thresholds
- StrategyConfig with tier-specific parameters
- RiskProfile with capital-aware settings
- AbsoluteReturnTarget with feasibility validation

## 2. Dependencies
- `decimal.Decimal`
- `enum.Enum`
- `typing.ClassVar`
- `typing.Dict`
- `typing.List`
- `typing.Optional`
- `pydantic.BaseModel`
- `pydantic.Field`

## 3. Core Components (Contracts)
### Public Classes
- `CapitalTier`: Must conform to application domain models.
- `CapitalTierThresholds`: Must conform to application domain models.
- `RiskProfile`: Must conform to application domain models.
- `StrategyFeatures`: Must conform to application domain models.
- `CapitalTierConfig`: Must conform to application domain models.
- `AbsoluteReturnTarget`: Must conform to application domain models.
- `AbsoluteReturnValidation`: Must conform to application domain models.
- `CapitalTierResult`: Must conform to application domain models.
- `Config`: Must conform to application domain models.
- `Config`: Must conform to application domain models.
### Public Functions
- `enabled_modules()`: Must return strictly typed objects.
- `target_annual_return_pct()`: Must return strictly typed objects.
- `required_alpha_monthly()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [ ] Must execute without syntax errors.
- [ ] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [ ] Exceptions must be handled.
