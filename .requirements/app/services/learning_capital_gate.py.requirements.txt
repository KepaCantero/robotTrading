# Requirements Document: services/learning_capital_gate.py

## 1. Module Overview
PHASE 0 - Learning Capital Gate (T0.2.1)

Prevents learning engine initialization on small accounts where the computational
cost of learning exceeds the alpha generated by learning-driven improvements.

Economics principle: Learning has implicit costs:
- Feature extraction and preprocessing
- Model training (compute, memory, time)
- Model retraining (regular updates)
- Hyperparameter search and optimization

These costs are especially problematic on small accounts where:
- Cost of learning > alpha generated by learning
- Better to use simple, deterministic strategies
- Capital is better deployed with trading rather than learning infrastructure

This gate enforces:
1. Minimum capital threshold (configurable) for learning to be economically viable
2. Learning cost-benefit ratio (learning cost must be < configured % of expected alpha)
3. Capital-aware learning configuration (adjusts learning frequency by account size)
4. Explicit warning when learning is disabled

## 2. Dependencies
- `logging`
- `decimal.Decimal`
- `typing.Dict`
- `typing.Tuple`
- `typing.TYPE_CHECKING`
- `app.shared.config.centralized_config.get_config`

## 3. Core Components (Contracts)
### Public Classes
- `LearningCapitalGate`: Must conform to application domain models.
### Public Functions
- `is_learning_viable()`: Must return strictly typed objects.
- `get_recommended_learning_config()`: Must return strictly typed objects.
- `get_minimum_capital_for_learning()`: Must return strictly typed objects.
- `log_learning_decision()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [ ] Exceptions must be handled.
