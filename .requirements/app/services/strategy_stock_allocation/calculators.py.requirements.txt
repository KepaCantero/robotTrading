# Requirements Document: services/strategy_stock_allocation/calculators.py

## 1. Module Overview
Statistical calculation modules.

Provides specialized calculators for Hurst exponent, half-life, and
stationarity testing following Single Responsibility Principle.

## 2. Dependencies
- `__future__.annotations`
- `logging`
- `typing.TYPE_CHECKING`
- `typing.Any`
- `numpy`
- `pandas`
- `app.shared.config.centralized_config.StockAllocationSettings`
- `statsmodels.regression.linear_model.OLS`
- `statsmodels.tsa.stattools.adfuller`
- `statsmodels.tsa.stattools.kpss`
- `warnings`
- `warnings`
- `warnings`

## 3. Core Components (Contracts)
### Public Classes
- `HurstCalculator`: Must conform to application domain models.
- `HalfLifeCalculator`: Must conform to application domain models.
- `StationarityTester`: Must conform to application domain models.
### Public Functions
- `OLS()`: Must return strictly typed objects.
- `calculate()`: Must return strictly typed objects.
- `calculate()`: Must return strictly typed objects.
- `test()`: Must return strictly typed objects.
- `OLS()`: Must return strictly typed objects.
- `adfuller()`: Must return strictly typed objects.
- `kpss()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [x] Exceptions must be handled.
