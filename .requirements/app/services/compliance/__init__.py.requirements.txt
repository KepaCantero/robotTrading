# Requirements Document: services/compliance/__init__.py

## 1. Module Overview
Regulatory Compliance Module for algoTrading System.

This module provides regulatory compliance tracking for different jurisdictions:
- PDT (Pattern Day Trader) rule enforcement for USA accounts
- Wash sale tracking for USA accounts
- Order pattern analysis (layering, spoofing, marking the close)
- Geographic restrictions and KYC compliance

IMPORTANT: Country-Specific Rules
- USA: PDT rule applies, wash sale rule applies
- Spain (ES): No PDT rule, no wash sale rule
- UK: Different regulatory framework
- EU: MiFID II regulations

Usage:
    from app.services.compliance import (
        ComplianceManager,
        Country,
        TradeRecord,
    )

    # Create compliance manager for Spain
    compliance = ComplianceManager(country=Country.ES)

    # Check if trade is allowed
    trade = TradeRecord(
        symbol="AAPL",
        side="BUY",
        quantity=Decimal("100"),
        price=Decimal("150"),
        trade_date=date.today(),
    )

    allowed, violations = compliance.check_trade_allowed(
        trade=trade,
        account_equity=Decimal("100000"),
    )

    if not allowed:
        logger.warning(f"Trade rejected: {violations}")

## 2. Dependencies
- `app.services.compliance.manager.ComplianceManager`
- `app.services.compliance.manager.ComplianceReport`
- `app.services.compliance.manager.ComplianceViolation`
- `app.services.compliance.manager.Country`
- `app.services.compliance.manager.TradeRecord`
- `app.services.compliance.order_pattern_analyzer.OrderPatternAnalyzer`
- `app.services.compliance.pdt_tracker.PDTTracker`
- `app.services.compliance.wash_sale_tracker.WashSaleTracker`

## 3. Core Components (Contracts)

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [ ] Exceptions must be handled.
