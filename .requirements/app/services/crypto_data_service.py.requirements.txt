# Requirements Document: services/crypto_data_service.py

## 1. Module Overview
Crypto Data Service - Servicio de datos para criptomonedas.

Proporciona:
- Datos de precio OHLCV para criptomonedas
- Soporte para BTC, ETH, y principales altcoins
- Integración con exchanges (Binance, Coinbase, Kraken)
- Caching y fallback defaults
- Reconnection with exponential backoff for 24/7 operation

## 2. Dependencies
- `asyncio`
- `logging`
- `datetime.datetime`
- `datetime.timedelta`
- `decimal.Decimal`
- `typing.Any`
- `typing.Dict`
- `typing.List`
- `typing.Optional`
- `requests.exceptions.HTTPError`
- `requests.exceptions.RequestException`
- `app.infrastructure.resilience.reconnection_manager.ReconnectionConfig`
- `app.infrastructure.resilience.reconnection_manager.ReconnectionManager`
- `app.core.timezone_utils.utc_now`
- `app.shared.config.centralized_config.get_config`

## 3. Core Components (Contracts)
### Public Classes
- `CryptoDataFetcher`: Must conform to application domain models.
### Public Functions
- `get_crypto_fetcher()`: Must return strictly typed objects.
- `reset_crypto_fetcher()`: Must return strictly typed objects.
- `get_available_cryptos()`: Must return strictly typed objects.
- `get_crypto_pairs()`: Must return strictly typed objects.
- `get_current_prices()`: Must return strictly typed objects.
- `get_historical_ohlcv()`: Must return strictly typed objects.
- `get_connection_stats()`: Must return strictly typed objects.
- `get_bid_ask_spread()`: Must return strictly typed objects.
- `get_risk_metrics()`: Must return strictly typed objects.
- `is_trading_hours()`: Must return strictly typed objects.
- `invalidate_cache()`: Must return strictly typed objects.
- `on_attempt()`: Must return strictly typed objects.
- `on_success()`: Must return strictly typed objects.
- `on_failure()`: Must return strictly typed objects.
- `alert_callback()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [x] Exceptions must be handled.
