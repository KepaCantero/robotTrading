# Requirements Document: services/smart_order_routing/smart_order_router.py

## 1. Module Overview
T2.1 - Smart Order Router

Main orchestrator for intelligent order routing of large positions (€25k+).

Coordinates:
1. Market impact estimation (via MarketImpactEstimator)
2. Commission negotiation (via BrokerNegotiationEngine)
3. Order splitting optimization (via OrderSplittingOptimizer)
4. Execution cost monitoring (via ExecutionCostMonitor)

Returns ExecutionPlan with optimized tranches and cost budgets.

## 2. Dependencies
- `logging`
- `decimal.Decimal`
- `typing.Dict`
- `typing.Optional`
- `broker_negotiation_engine.get_broker_negotiation_engine`
- `execution_cost_monitor.get_execution_cost_monitor`
- `market_impact_estimator.get_market_impact_estimator`
- `models.ExecutionPlan`
- `order_splitting_optimizer.get_order_splitting_optimizer`

## 3. Core Components (Contracts)
### Public Classes
- `SmartOrderRouter`: Must conform to application domain models.
### Public Functions
- `get_smart_order_router()`: Must return strictly typed objects.
- `get_cost_forecast()`: Must return strictly typed objects.
- `validate_execution_plan()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [x] Exceptions must be handled.
