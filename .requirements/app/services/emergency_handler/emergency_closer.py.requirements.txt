# Requirements Document: services/emergency_handler/emergency_closer.py

## 1. Module Overview
Emergency Closer - Closes all positions on critical system failures.

Protects against catastrophic losses by closing all positions when:
- Broker connection is lost
- System is shutting down
- Critical errors occur
- Manual emergency trigger is activated

Uses centralized configuration for all timeout parameters.

## 2. Dependencies
- `asyncio`
- `logging`
- `signal`
- `dataclasses.dataclass`
- `dataclasses.field`
- `datetime.datetime`
- `decimal.Decimal`
- `enum.Enum`
- `typing.Any`
- `typing.Callable`
- `typing.Dict`
- `typing.List`
- `typing.Optional`
- `requests.exceptions.HTTPError`
- `app.shared.config.centralized_config.get_config`
- `app.shared.utils.timezone_utils.utc_now`

## 3. Core Components (Contracts)
### Public Classes
- `EmergencyTrigger`: Must conform to application domain models.
- `EmergencyCloseResult`: Must conform to application domain models.
- `EmergencyCloser`: Must conform to application domain models.
### Public Functions
- `to_dict()`: Must return strictly typed objects.
- `get_audit_log()`: Must return strictly typed objects.
- `get_last_trigger()`: Must return strictly typed objects.
- `get_last_close_time()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [x] Exceptions must be handled.
