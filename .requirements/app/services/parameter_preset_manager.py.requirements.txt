# Requirements Document: services/parameter_preset_manager.py

## 1. Module Overview
Parameter Preset Manager - TASK-PARAM-1

Manages and loads parameter presets (Conservative/Aggressive/Balanced)
with sensible ranges per indicator (TASK-PARAM-3).

## 2. Dependencies
- `logging`
- `pathlib.Path`
- `typing.Any`
- `typing.Dict`
- `typing.List`
- `typing.Optional`
- `yaml`

## 3. Core Components (Contracts)
### Public Classes
- `ParameterPresetManager`: Must conform to application domain models.
### Public Functions
- `get_preset_manager()`: Must return strictly typed objects.
- `get_preset()`: Must return strictly typed objects.
- `get_strategy_params()`: Must return strictly typed objects.
- `get_indicator_range()`: Must return strictly typed objects.
- `get_all_presets()`: Must return strictly typed objects.
- `get_preset_description()`: Must return strictly typed objects.
- `validate_parameter_in_range()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [x] Exceptions must be handled.
