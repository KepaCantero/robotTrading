# Requirements Document: shared/performance/metrics.py

## 1. Module Overview
Unified Performance Metrics Module

This is THE SINGLE SOURCE OF TRUTH for all performance metric calculations.
It re-exports from the consolidated domain implementation and provides
config-aware wrappers.

ARCHITECTURE:
    - Domain implementation: app/domain/services/metrics/performance_metrics.py
    - Numba-accelerated: app/backtesting/numba_metrics.py
    - This module: Unified interface with config integration

USAGE:
    from app.shared.performance.metrics import (
        sharpe_ratio,
        sortino_ratio,
        max_drawdown,
        PerformanceMetricsCalculator,
    )

    # Using convenience functions (reads defaults from CentralizedConfig)
    sr = sharpe_ratio(returns)

    # Using calculator with custom config
    calc = PerformanceMetricsCalculator.from_config()
    sr = calc.sharpe_ratio(returns)

Reference:
    - Lopez de Prado, M. (2020). Machine Learning for Asset Managers.
    - Chan, E.P. (2013). Algorithmic Trading.

## 2. Dependencies
- `typing.List`
- `typing.Optional`
- `typing.Union`
- `numpy`
- `pandas`
- `decimal.Decimal`
- `app.domain.services.metrics.performance_metrics.PerformanceMetricsCalculator`
- `app.domain.services.metrics.performance_metrics.SharpeRatioResult`
- `app.domain.services.metrics.performance_metrics.DrawdownResult`
- `app.domain.services.metrics.performance_metrics.PerformanceResult`
- `app.domain.services.metrics.performance_metrics.get_sharpe_ratio`
- `app.domain.services.metrics.performance_metrics.get_sortino_ratio`
- `app.domain.services.metrics.performance_metrics.get_calmar_ratio`
- `app.domain.services.metrics.performance_metrics.get_omega_ratio`
- `app.domain.services.metrics.performance_metrics.get_max_drawdown`
- `app.domain.services.metrics.performance_metrics.get_ulcer_index`
- `app.shared.config.centralized_config.get_config`

## 3. Core Components (Contracts)
### Public Classes
- `PerformanceMetricsCalculator`: Must conform to application domain models.
### Public Functions
- `sharpe_ratio()`: Must return strictly typed objects.
- `sortino_ratio()`: Must return strictly typed objects.
- `calmar_ratio()`: Must return strictly typed objects.
- `omega_ratio()`: Must return strictly typed objects.
- `max_drawdown()`: Must return strictly typed objects.
- `ulcer_index()`: Must return strictly typed objects.
- `from_config()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [ ] Must execute without syntax errors.
- [ ] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [ ] Exceptions must be handled.
