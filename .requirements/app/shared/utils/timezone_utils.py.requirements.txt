# Requirements Document: shared/utils/timezone_utils.py

## 1. Module Overview
Timezone utilities for multi-market trading system.

Ensures consistent timezone handling across stocks (US/EU), forex (24/7),
and crypto (24/7) markets.

Phase 0.3: Timezone Awareness Implementation
- All timestamps use timezone-aware datetime
- Consistent UTC handling across all services
- Market-specific timezone support

## 2. Dependencies
- `logging`
- `datetime.datetime`
- `datetime.timezone`
- `typing.Optional`
- `tzlocal`

## 3. Core Components (Contracts)
### Public Functions
- `utc_now()`: Must return strictly typed objects.
- `to_utc()`: Must return strictly typed objects.
- `to_market_time()`: Must return strictly typed objects.
- `format_utc()`: Must return strictly typed objects.
- `format_market_time()`: Must return strictly typed objects.
- `get_market_timezone()`: Must return strictly typed objects.
- `is_market_open()`: Must return strictly typed objects.
- `get_market_open_close_time()`: Must return strictly typed objects.
- `validate_timezone_aware()`: Must return strictly typed objects.
- `ensure_timezone_aware()`: Must return strictly typed objects.
- `parse_iso_datetime()`: Must return strictly typed objects.
- `get_db_timestamp_default()`: Must return strictly typed objects.
- `get_db_timestamp_onupdate()`: Must return strictly typed objects.
- `to_local_timezone()`: Must return strictly typed objects.
- `format_for_display()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [x] Exceptions must be handled.
