# Requirements Document: shared/config/params/trading_thresholds.py

## 1. Module Overview
Trading Thresholds Configuration

Extracted from centralized_config.py for SRP compliance.
Contains all trading-related thresholds and parameters.

TASK-10: Centralización de Configuración
TASK-24: SRP Refactoring

## 2. Dependencies
- `logging`
- `decimal.Decimal`
- `typing.Dict`
- `pydantic.BaseModel`
- `pydantic.Field`
- `pydantic.field_validator`

## 3. Core Components (Contracts)
### Public Classes
- `TradingThresholds`: Must conform to application domain models.
### Public Functions
- `validate_percentage()`: Must return strictly typed objects.
- `validate_percentage_limits()`: Must return strictly typed objects.
- `validate_stop_loss()`: Must return strictly typed objects.
- `validate_decimal_percentage()`: Must return strictly typed objects.
- `validate_score()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [x] Exceptions must be handled.
