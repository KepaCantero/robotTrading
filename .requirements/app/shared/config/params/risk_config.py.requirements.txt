# Requirements Document: shared/config/params/risk_config.py

## 1. Module Overview
Risk and Compliance Configuration

Extracted from centralized_config.py for SRP compliance.
Contains CurrencyHedgingConfig, SectorCountryDiversificationConfig, ComplianceConfig.

TASK-10: Centralización de Configuración
TASK-24: SRP Refactoring

## 2. Dependencies
- `logging`
- `decimal.Decimal`
- `typing.Any`
- `typing.Dict`
- `typing.List`
- `typing.Optional`
- `pydantic.BaseModel`
- `pydantic.Field`
- `pydantic.field_validator`
- `pydantic.model_validator`

## 3. Core Components (Contracts)
### Public Classes
- `CurrencyHedgingConfig`: Must conform to application domain models.
- `SectorCountryDiversificationConfig`: Must conform to application domain models.
- `ComplianceConfig`: Must conform to application domain models.
### Public Functions
- `validate_score_100()`: Must return strictly typed objects.
- `validate_score_1()`: Must return strictly typed objects.
- `validate_spread_bps()`: Must return strictly typed objects.
- `validate_liquidity_weights_sum()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [x] Exceptions must be handled.
