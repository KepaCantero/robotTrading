# Requirements Document: shared/config/compliance.py

## 1. Module Overview
Compliance Configuration Module

Contains configuration for the Compliance Engine including thresholds
for statistical learning, meta-labeling, microstructure, risk management,
and execution quality.

## 2. Dependencies
- `pydantic.BaseModel`
- `pydantic.Field`
- `pydantic.field_validator`
- `pydantic.model_validator`
- `app.shared.config.base.ConfigBase`

## 3. Core Components (Contracts)
### Public Classes
- `ComplianceConfig`: Must conform to application domain models.
- `SpainTaxConfig`: Must conform to application domain models.
### Public Functions
- `validate_score_100()`: Must return strictly typed objects.
- `validate_score_1()`: Must return strictly typed objects.
- `validate_spread_bps()`: Must return strictly typed objects.
- `validate_liquidity_weights_sum()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [ ] Must execute without syntax errors.
- [ ] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [x] Exceptions must be handled.
