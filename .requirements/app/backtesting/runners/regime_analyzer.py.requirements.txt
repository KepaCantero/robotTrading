# Requirements Document: backtesting/runners/regime_analyzer.py

## 1. Module Overview
Regime Analyzer Module

Extracted from comprehensive_backtest_runner.py for SRP compliance.

Provides regime detection and analysis functionality for backtesting:
- Simple regime detection based on returns and volatility
- Regime name mapping based on characteristics
- Regime transition analysis using Markov chains

## 2. Dependencies
- `logging`
- `typing.Any`
- `typing.Dict`
- `numpy`
- `pandas`

## 3. Core Components (Contracts)
### Public Classes
- `RegimeAnalyzer`: Must conform to application domain models.
### Public Functions
- `detect_regimes()`: Must return strictly typed objects.
- `detect_simple_regimes()`: Must return strictly typed objects.
- `get_regime_name_mapping()`: Must return strictly typed objects.
- `analyze_regime_transitions()`: Must return strictly typed objects.
- `get_regime_statistics()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [ ] Exceptions must be handled.
