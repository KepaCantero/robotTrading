# Requirements: backtesting/signal_diagnostic_logger.py

## Audit Status: PASSED
**Audit Date:** 2026-02-07T05:30:00Z
**Auditor:** GAP Audit System

## Source File Analysis
- **File Path:** `app/backtesting/signal_diagnostic_logger.py`
- **Lines of Code:** 211
- **Type:** Module

## Purpose
Diagnostic logger for multi-strategy backtesting. Tracks detailed information about signal generation, rejection reasons, and strategy-level metrics for diagnostic purposes.

## Dependencies
### Internal
- None

### External
- `json` - JSON serialization
- `logging` - Logging
- `collections.defaultdict` - Default dict
- `collections.DefaultDict` - Type hint
- `datetime` - Timestamp handling
- `pathlib.Path` - Path handling
- `typing` - Type hints

## Classes/Functions
### Classes
- `SignalDiagnosticLogger` - Diagnostic logger class

  #### Methods
  - `__init__(output_dir)` - Initialize logger with output directory
  - `log_signal_candidate(strategy_name, symbol, signal_type, metadata)` - Log signal candidate
  - `log_signal_rejected(strategy_name, symbol, reason, failed_check, metadata)` - Log rejected signal
  - `log_signal_executed(strategy_name, symbol, metadata)` - Log executed signal
  - `get_strategy_summary(strategy_name)` - Get summary for a strategy
  - `get_all_summaries()` - Get summaries for all strategies
  - `save_diagnostic_report(filename)` - Save report to JSON file
  - `print_summary()` - Log summary (logs to file, not console)
  - `reset()` - Reset all counters

## BASE_RULES Compliance
✅ **R099 (Absolute imports):** No relative imports
✅ **R098 (No relative imports):** All imports are from standard library
✅ **R100 (Modern type hints):** Uses `Path`, `Dict[str, Any]`, `List[str]`, `Optional[Dict[str, Any]]`
⚠️ **R102 (Any without docs):** `Dict[str, Any]` used but documented and acceptable for flexible metadata
✅ **R103 (No type comments):** No type comments used
✅ **R104 (No bare except):** N/A (no exception handling)
✅ **R105 (No print statements):** Uses `logger` instead of print (even `print_summary` logs to file)
✅ **R107 (No mutable defaults):**
  - `output_dir: Path = Path("...")` (creates new Path, mutable but default is factory)
  - `metadata: Optional[Dict[str, Any]] = None` (immutable)
  - `filename: str = None` (immutable)
✅ **R108 (Exception handling):** N/A (no exception handling)
✅ **R110 (Google docstrings):** All classes and methods have Google-style docstrings
✅ **R111 (No circular imports):** No circular imports detected

## Type Hints Analysis
- Uses `Optional[T]` syntax (traditional)
- `Dict[str, Any]` appropriately used for flexible metadata
- `DefaultDict[str, Dict[str, Any]]` properly annotated
- Return types specified for all methods
- `List[str]` properly used for tags

## Key Features
### Strategy-Level Tracking
- `signals_candidate` - Signals generated by strategy
- `signals_rejected_risk` - Rejected by risk check
- `signals_rejected_filter` - Rejected by sector/filter
- `signals_executed` - Successfully executed
- `checks_failed` - Track which checks fail

### Data Storage
- `rejection_details` - List of detailed rejection records
- `candidate_details` - List of candidate signal records
- `symbol_stats` - Per-symbol tracking

### Output
- JSON diagnostic report with all data
- Summary log file with statistics
- All candidates and rejections preserved (not truncated)

## Notes
- `print_summary()` method is misnamed - it logs to file, not console
- Method includes emoji in output (line 171: 📊)
- No exception handling for file operations
- Default output_dir is hardcoded path

---
*Auto-generated on Thu Feb  5 20:32:58 CET 2026*
*Updated for GAP Audit on 2026-02-07*
