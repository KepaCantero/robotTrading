# Requirements Document: simulation/microstructure.py

## 1. Module Overview
Market Microstructure Analysis - Larry Harris's Order Flow and Liquidity Models

This module implements market microstructure concepts from Harris,
Chapter 10: "Market Information and Price Discovery" and Chapter 11:
"Liquidity and Liquidity Risk."

Key concepts implemented:
1. Order flow analysis
2. Market depth profiling
3. Liquidity provision
4. Order imbalance indicators
5. Price impact functions

Reference:
    Harris, L. (2003). Trading and Exchanges, Chapters 10-11.

## 2. Dependencies
- `logging`
- `collections.deque`
- `dataclasses.dataclass`
- `datetime.datetime`
- `decimal.Decimal`
- `enum.Enum`
- `typing.Dict`
- `typing.Optional`
- `typing.Tuple`
- `numpy`
- `order_book.LimitOrderBook`
- `order_book.Order`
- `order_book.OrderSide`
- `order_book.Trade`

## 3. Core Components (Contracts)
### Public Classes
- `OrderFlowDirection`: Must conform to application domain models.
- `LiquidityRegime`: Must conform to application domain models.
- `OrderImbalance`: Must conform to application domain models.
- `PriceImpactFunction`: Must conform to application domain models.
- `MarketMicrostructureMetrics`: Must conform to application domain models.
- `OrderFlowAnalyzer`: Must conform to application domain models.
- `MarketDepthAnalyzer`: Must conform to application domain models.
- `LiquidityProvider`: Must conform to application domain models.
- `MarketMicrostructureAnalyzer`: Must conform to application domain models.
### Public Functions
- `create_market_microstructure_analyzer()`: Must return strictly typed objects.
- `is_biased()`: Must return strictly typed objects.
- `bias_strength()`: Must return strictly typed objects.
- `calculate_impact()`: Must return strictly typed objects.
- `is_toxic()`: Must return strictly typed objects.
- `is_illiquid()`: Must return strictly typed objects.
- `add_order()`: Must return strictly typed objects.
- `add_trade()`: Must return strictly typed objects.
- `calculate_order_imbalance()`: Must return strictly typed objects.
- `calculate_flow_toxicity()`: Must return strictly typed objects.
- `detect_informed_trading()`: Must return strictly typed objects.
- `analyze_depth()`: Must return strictly typed objects.
- `estimate_price_impact()`: Must return strictly typed objects.
- `classify_liquidity_regime()`: Must return strictly typed objects.
- `position()`: Must return strictly typed objects.
- `pnl()`: Must return strictly typed objects.
- `should_provide_liquidity()`: Must return strictly typed objects.
- `calculate_quotes()`: Must return strictly typed objects.
- `on_trade_executed()`: Must return strictly typed objects.
- `mark_to_market()`: Must return strictly typed objects.
- `update()`: Must return strictly typed objects.
- `get_comprehensive_metrics()`: Must return strictly typed objects.

## 4. Execution Requirements (Compliance Rules)
- [x] Must execute without syntax errors.
- [x] Must contain explicit structured logging or print interception.
- [x] Must not expose external secrets.
- [ ] Exceptions must be handled.
