# Core Web Framework
fastapi>=0.104.0,<0.110.0
uvicorn[standard]>=0.24.0,<0.30.0
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0

# Database
sqlalchemy>=2.0.0,<3.0.0
alembic>=1.12.0,<2.0.0
psycopg2-binary>=2.9.0,<3.0.0

# Data Processing & Analysis
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<3.0.0  # Updated to allow numpy 2.x for pandas-ta-classic compatibility
pandas-ta-classic>=0.3.36,<1.0.0  # Technical Analysis Library (REQUIRED - compatible with Python 3.9+, uses library ONLY, NO manual calculations)
scipy>=1.11.0,<2.0.0  # Statistical calculations (cointegration, etc.)
statsmodels>=0.14.0,<1.0.0  # Statistical tests (ADF, KPSS, Johansen, Engle-Granger)
arch>=6.0.0,<7.0.0  # GARCH volatility modeling

# Market Data
yfinance>=0.2.0,<1.0.0
yahoo-fin>=0.8.9,<1.0.0  # Alternative to yfinance (yahoo_fin)
requests-html>=0.10.0,<1.0.0  # Required by yahoo_fin

# Configuration
pyyaml>=6.0,<7.0

# Optimization (NEW - Multi-Strategy Optimizer)
optuna>=3.4.0,<4.0.0

# Machine Learning - Supervised Learning (REQUIRED for SupervisedLearningEngine)
scikit-learn>=1.3.0,<2.0.0
xgboost>=2.0.0,<3.0.0  # Optional but recommended for better performance
lightgbm>=4.0.0,<5.0.0  # Optional but recommended for better performance
catboost>=1.2.0,<2.0.0  # Optional but recommended for better performance
shap>=0.42.0,<1.0.0  # Optional but recommended for feature importance analysis (SHAP values)

# Deep Learning - PyTorch (REQUIRED for DeepLearningEngine)
torch>=2.0.0,<3.0.0  # PyTorch core
torchvision>=0.15.0,<1.0.0  # Optional, for data transforms

# Deep Learning - TensorFlow (Alternative backend for DeepLearningEngine)
tensorflow>=2.13.0,<3.0.0  # Optional alternative to PyTorch

# Reinforcement Learning (REQUIRED for ReinforcementLearningEngine)
stable-baselines3>=2.0.0,<3.0.0  # RL algorithms (PPO, A2C, DDPG, etc.)
gym>=0.26.0,<1.0.0  # RL environments
gymnasium>=0.29.0,<1.0.0  # Newer gym API (optional)

# HTTP & API Clients
httpx>=0.25.0,<1.0.0
requests>=2.31.0,<3.0.0

# Redis (for caching if used)
redis>=5.0.0,<6.0.0

# Logging & Monitoring
python-json-logger>=2.0.0,<3.0.0

# CORS & Security
python-multipart>=0.0.6,<1.0.0

# Performance Analytics & Reporting (Fase 1 - Awesome-Quant)
quantstats>=0.0.62,<1.0.0  # Portfolio analytics and HTML reports
empyrical-reloaded>=0.5.0,<1.0.0  # Risk and performance metrics (standard library)
pyfolio-reloaded>=0.9.5,<1.0.0  # Portfolio and risk analytics with tearsheets

