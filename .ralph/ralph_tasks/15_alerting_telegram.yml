# ============================================
# Ralph Task 15: Alerting Telegram
# ============================================
# PRIORIDAD: P1
# FASE: Phase 5 - User Interface Layer
# OBJETIVO: Implement Telegram notification channel
# TIEMPO: ~6 horas
#
# Depends on: None (independent task)
# ============================================================================

event_loop:
  prompt_file: ".ralph/ralph_tasks/prompts/15_alerting_telegram.md"
  completion_promise: "TELEGRAM_ALERTING_COMPLETE"
  max_iterations: 100
  max_runtime_seconds: 21600  # 6 hours
  checkpoint_interval: 3
  starting_event: "telegram.start"

cli:
  backend: "claude"

core:
  specs_dir: "./specs/"

hats:
  telegram_implementer:
    name: "Telegram Alerting Implementer"
    description: "Implements Telegram notification channel for alerting system"
    triggers: ["telegram.start", "telegram.next_step"]
    publishes: ["telegram.next_step", "telegram.complete", "telegram.blocked"]
    default_publishes: "telegram.next_step"
    instructions: |
      ## TELEGRAM ALERTING IMPLEMENTATION

      **OBJETIVO:** Implement Telegram notification channel for the alerting system.

      ### EXISTING INFRASTRUCTURE

      The project already has:
      - `app/services/alerting_system/` - Complete alerting system
      - `app/services/alerting_system/models.py` - Has `NotificationChannelType` enum (WEBHOOK, EMAIL, SLACK, DISCORD)
      - `app/services/alerting_system/notification_channels.py` - Has WebhookChannel, EmailChannel, SlackChannel, DiscordChannel
      - `app/services/alerting_system/notification_channels.py` - Has `NotificationDispatcher` that dispatches to multiple channels
      - `app/user_config/user_settings.py` - Has `NotificationSettings` with `enable_telegram` and `telegram_chat_id` fields

      ### TASK REQUIREMENTS

      Task 15 needs to:
      1. Add TELEGRAM to `NotificationChannelType` enum
      2. Create `TelegramChannel` class in `notification_channels.py`
      3. Register `TelegramChannel` in `NotificationDispatcher`
      4. Create Telegram bot helper for setup and testing
      5. Integrate with UserSettings for Telegram configuration
      6. Add tests for Telegram channel

      ### PASO 1: Update NotificationChannelType Enum

      Archivo: `app/services/alerting_system/models.py`

      Add `TELEGRAM` to the `NotificationChannelType` enum:

      ```python
      class NotificationChannelType(str, Enum):
          """Supported notification channels."""

          WEBHOOK = "webhook"
          EMAIL = "email"
          SLACK = "slack"
          DISCORD = "discord"
          TELEGRAM = "telegram"  # ADD THIS
      ```

      ### PASO 2: Create TelegramChannel Class

      Archivo: `app/services/alerting_system/notification_channels.py`

      Add the TelegramChannel class:

      ```python
      class TelegramChannel(NotificationChannel):
          """Telegram notification channel using Bot API."""

          async def send(self, target: NotificationTarget, payload: NotificationPayload) -> bool:
              """Send notification via Telegram Bot API."""
              if not target.enabled:
                  logger.debug(f"Telegram channel disabled")
                  return False

              try:
                  import httpx

                  # Extract bot token and chat_id from endpoint
                  # Format: "bot_token:chat_id" or use headers
                  bot_token = target.headers.get("telegram_bot_token", "")
                  chat_id = target.headers.get("telegram_chat_id", target.endpoint)

                  if not bot_token or not chat_id:
                      logger.error("Telegram bot_token or chat_id missing")
                      return False

                  # Build message with Markdown formatting
                  emoji_map = {
                      "info": "ℹ️",
                      "warning": "⚠️",
                      "critical": "🚨",
                  }
                  emoji = emoji_map.get(payload.severity.value, "📊")

                  message = f"{emoji} *{payload.rule_name}*\n\n"
                  message += f"*Severity:* {payload.severity.value.upper()}\n"
                  message += f"*Message:* {payload.message}\n"
                  message += f"*Metric:* {payload.metric_name}\n"

                  if payload.metric_value:
                      message += f"*Value:* {payload.metric_value}\n"

                  if payload.symbol:
                      message += f"*Symbol:* {payload.symbol}\n"

                  message += f"\n_:{payload.triggered_at.strftime('%Y-%m-%d %H:%M:%S UTC')}_"

                  # Send via Telegram Bot API
                  url = f"https://api.telegram.org/bot{bot_token}/sendMessage"

                  async with httpx.AsyncClient(timeout=target.timeout_seconds) as client:
                      response = await client.post(
                          url,
                          json={
                              "chat_id": chat_id,
                              "text": message,
                              "parse_mode": "Markdown",
                              "disable_web_page_preview": True,
                          },
                      )

                      if response.status_code == 200:
                          logger.info(f"Telegram message sent to chat_id: {chat_id}")
                          return True
                      else:
                          logger.warning(
                              f"Telegram send failed (status: {response.status_code}): {response.text}"
                          )
                          return False

              except (ConnectionError, TimeoutError, OSError) as e:
                  logger.error(f"Telegram error: {e}")
                  return False
      ```

      ### PASO 3: Register TelegramChannel in NotificationDispatcher

      Update the `NotificationDispatcher.__init__` method:

      ```python
      def __init__(self):
          """Initialize dispatcher."""
          self.channels = {
              NotificationChannelType.WEBHOOK: WebhookChannel(),
              NotificationChannelType.EMAIL: EmailChannel(),
              NotificationChannelType.SLACK: SlackChannel(),
              NotificationChannelType.DISCORD: DiscordChannel(),
              NotificationChannelType.TELEGRAM: TelegramChannel(),  # ADD THIS
          }
          # ... rest of init
      ```

      ### PASO 4: Create Telegram Bot Helper

      Archivo: `app/services/alerting_system/telegram_helper.py`

      ```python
      """
      Telegram Bot Helper

      Helper utilities for setting up and testing Telegram bot integration.
      """
      import asyncio
      import logging
      from typing import Optional

      import httpx

      logger = logging.getLogger(__name__)


      class TelegramBotHelper:
          """
          Helper for Telegram bot operations.

          Usage:
              1. Create a bot via @BotFather on Telegram
              2. Get the bot token
              3. Use this helper to get your chat_id
              4. Configure bot_token and chat_id in UserSettings
          """

          API_BASE = "https://api.telegram.org/bot"

          @classmethod
          async def test_bot_token(cls, bot_token: str) -> dict:
              """
              Test if a bot token is valid.

              Args:
                  bot_token: Telegram bot token from @BotFather

              Returns:
                  dict with 'valid' bool and 'bot_info' if valid
              """
              try:
                  url = f"{cls.API_BASE}{bot_token}/getMe"

                  async with httpx.AsyncClient(timeout=10) as client:
                      response = await client.get(url)

                  if response.status_code == 200:
                      data = response.json()
                      if data.get("ok"):
                          return {
                              "valid": True,
                              "bot_info": data.get("result", {}),
                          }
                      return {"valid": False, "error": data.get("description", "Unknown error")}

                  return {"valid": False, "error": f"HTTP {response.status_code}"}

              except Exception as e:
                  return {"valid": False, "error": str(e)}

          @classmethod
          async def get_chat_id(cls, bot_token: str) -> Optional[int]:
              """
              Get the chat_id by sending a test message and polling updates.

              Instructions:
                  1. Start a chat with your bot on Telegram
                  2. Send any message to the bot (e.g., "/start")
                  3. Call this method to get the chat_id

              Args:
                  bot_token: Telegram bot token from @BotFather

              Returns:
                  chat_id or None if no messages found
              """
              try:
                  url = f"{cls.API_BASE}{bot_token}/getUpdates"

                  async with httpx.AsyncClient(timeout=10) as client:
                      response = await client.get(url)

                  if response.status_code == 200:
                      data = response.json()
                      if data.get("ok"):
                          updates = data.get("result", [])
                          if updates:
                              # Get the most recent message's chat_id
                              return updates[-1].get("message", {}).get("chat", {}).get("id")

                  return None

              except Exception as e:
                  logger.error(f"Error getting chat_id: {e}")
                  return None

          @classmethod
          async def send_test_message(
              cls, bot_token: str, chat_id: int, message: str = "AlgoTrading bot test message!"
              ) -> bool:
              """
              Send a test message via Telegram.

              Args:
                  bot_token: Telegram bot token
                  chat_id: Telegram chat ID
                  message: Test message to send

              Returns:
                  True if message sent successfully
              """
              try:
                  url = f"{cls.API_BASE}{bot_token}/sendMessage"

                  async with httpx.AsyncClient(timeout=10) as client:
                      response = await client.post(
                          url,
                          json={
                              "chat_id": chat_id,
                              "text": message,
                              "parse_mode": "Markdown",
                          },
                      )

                  if response.status_code == 200:
                      data = response.json()
                      return data.get("ok", False)

                  return False

              except Exception as e:
                  logger.error(f"Error sending test message: {e}")
                  return False

          @classmethod
          def get_setup_instructions(cls) -> str:
              """Return instructions for setting up Telegram bot."""
              return """
      # Telegram Bot Setup for AlgoTrading Alerts

      ## Step 1: Create a Bot via @BotFather

      1. Open Telegram and search for @BotFather
      2. Send /newbot command
      3. Follow instructions to name your bot (e.g., "MyAlgoTradingBot")
      4. Copy the bot token (looks like: 123456789:ABCdefGHIjklMNOpqrsTUVwxyz)

      ## Step 2: Get Your Chat ID

      1. Start a chat with your bot on Telegram
      2. Send any message (e.g., /start)
      3. Use the get_chat_id() method to retrieve your chat_id

      ## Step 3: Configure in User Settings

      Update your user_config.yaml:

      ```yaml
      notifications:
        enable_telegram: true
        telegram_chat_id: "YOUR_CHAT_ID"
        telegram_bot_token: "YOUR_BOT_TOKEN"  # Store securely!
        alert_on_entry: true
        alert_on_exit: true
        alert_on_risk: true
        daily_summary: true
      ```

      ## Security Notes

      - Never commit your bot_token to version control
      - Use environment variables for sensitive data
      - Consider using a secrets manager for production
              """


      async def setup_telegram_bot_interactive() -> dict:
          """
          Interactive setup helper for Telegram bot.

          Returns:
              dict with bot_token and chat_id if successful
          """
          print("=== Telegram Bot Setup ===\n")
          print(TelegramBotHelper.get_setup_instructions())

          bot_token = input("\nEnter your bot token from @BotFather: ").strip()

          # Test bot token
          print("\nTesting bot token...")
          result = await TelegramBotHelper.test_bot_token(bot_token)

          if not result.get("valid"):
              print(f"❌ Invalid bot token: {result.get('error')}")
              return {}

          bot_info = result.get("bot_info", {})
          print(f"✅ Bot validated: @{bot_info.get('username')} ({bot_info.get('first_name')})")

          # Get chat_id
          print("\n" + "="*50)
          print("STEP 2: Get Your Chat ID")
          print("="*50)
          print("1. Open Telegram and start a chat with your bot")
          print("2. Send any message to the bot (e.g., /start)")
          input("\nPress Enter after sending a message to your bot...")

          print("Retrieving chat_id...")
          chat_id = await TelegramBotHelper.get_chat_id(bot_token)

          if not chat_id:
              print("❌ Could not retrieve chat_id. Make sure you sent a message to your bot.")
              return {}

          print(f"✅ Your chat_id: {chat_id}")

          # Send test message
          print("\nSending test message...")
          success = await TelegramBotHelper.send_test_message(bot_token, chat_id)

          if success:
              print("✅ Test message sent! Check your Telegram.")
          else:
              print("❌ Failed to send test message.")

          return {
              "bot_token": bot_token,
              "chat_id": str(chat_id),
          }
      ```

      ### PASO 5: Create CLI Script for Telegram Setup

      Archivo: `scripts/setup_telegram.py`

      ```python
      #!/usr/bin/env python3
      """
      Telegram Bot Setup Script

      Interactive script to set up Telegram bot for alerting.
      """
      import asyncio
      import sys
      from pathlib import Path

      project_root = Path(__file__).parent.parent
      sys.path.insert(0, str(project_root))

      from app.services.alerting_system.telegram_helper import (
          TelegramBotHelper,
          setup_telegram_bot_interactive,
      )


      async def main():
          """Main setup entry point."""
          print("\n" + "="*60)
          print("  AlgoTrading - Telegram Bot Setup")
          print("="*60 + "\n")

          # Run interactive setup
          config = await setup_telegram_bot_interactive()

          if config:
              print("\n" + "="*60)
              print("Setup Complete!")
              print("="*60)
              print("\nAdd this to your user_config.yaml:")
              print("-" * 60)
              print(f"notifications:")
              print(f"  enable_telegram: true")
              print(f"  telegram_chat_id: \"{config['chat_id']}\"")
              print(f"  # Store bot_token securely (use env vars!)")
              print(f"  telegram_bot_token: \"{config['bot_token']}\"")
              print("-" * 60)

          else:
              print("\nSetup failed. Please try again.")


      if __name__ == "__main__":
          asyncio.run(main())
      ```

      ### PASO 6: Create Tests

      Archivo: `tests/unit/alerting_system/test_telegram_channel.py`

      ```python
      import pytest
      from unittest.mock import AsyncMock, MagicMock, patch

      from app.services.alerting_system.models import (
          AlertSeverity,
          NotificationChannelType,
          NotificationPayload,
          NotificationTarget,
      )
      from app.services.alerting_system.notification_channels import TelegramChannel


      @pytest.fixture
      def telegram_channel():
          """Fixture for TelegramChannel."""
          return TelegramChannel()


      @pytest.fixture
      def notification_target():
          """Fixture for NotificationTarget with Telegram config."""
          return NotificationTarget(
              channel_type=NotificationChannelType.TELEGRAM,
              endpoint="123456789",  # chat_id
              enabled=True,
              headers={
                  "telegram_bot_token": "test_bot_token",
                  "telegram_chat_id": "123456789",
              },
          )


      @pytest.fixture
      def notification_payload():
          """Fixture for NotificationPayload."""
          from datetime import datetime
          from decimal import Decimal

          return NotificationPayload(
              event_id="test_event_1",
              rule_id="test_rule",
              rule_name="Test Alert Rule",
              severity=AlertSeverity.WARNING,
              message="This is a test alert message",
              metric_name="test_metric",
              metric_value=Decimal("123.45"),
              symbol="AAPL",
              triggered_at=datetime.utcnow(),
          )


      @pytest.mark.asyncio
      async def test_telegram_send_success(telegram_channel, notification_target, notification_payload):
          """Test successful Telegram message sending."""
          with patch("httpx.AsyncClient") as mock_client:
              mock_response = MagicMock()
              mock_response.status_code = 200
              mock_response.json.return_value = {"ok": True}

              mock_post = AsyncMock(return_value=mock_response)
              mock_client.return_value.__aenter__.return_value.post = mock_post

              result = await telegram_channel.send(notification_target, notification_payload)

              assert result is True
              mock_post.assert_called_once()

              # Verify call arguments
              call_args = mock_post.call_args
              assert "sendMessage" in call_args[0][0]
              assert call_args[1]["json"]["chat_id"] == "123456789"
              assert "parse_mode" in call_args[1]["json"]


      @pytest.mark.asyncio
      async def test_telegram_send_disabled(telegram_channel, notification_target, notification_payload):
          """Test that disabled channel doesn't send."""
          notification_target.enabled = False

          with patch("httpx.AsyncClient") as mock_client:
              mock_post = AsyncMock()
              mock_client.return_value.__aenter__.return_value.post = mock_post

              result = await telegram_channel.send(notification_target, notification_payload)

              assert result is False
              mock_post.assert_not_called()


      @pytest.mark.asyncio
      async def test_telegram_send_missing_token(telegram_channel, notification_target, notification_payload):
          """Test handling of missing bot token."""
          notification_target.headers = {}  # Remove bot_token

          with patch("httpx.AsyncClient") as mock_client:
              mock_post = AsyncMock()
              mock_client.return_value.__aenter__.return_value.post = mock_post

              result = await telegram_channel.send(notification_target, notification_payload)

              assert result is False
              mock_post.assert_not_called()


      @pytest.mark.asyncio
      async def test_telegram_send_api_error(telegram_channel, notification_target, notification_payload):
          """Test handling of API error response."""
          with patch("httpx.AsyncClient") as mock_client:
              mock_response = MagicMock()
              mock_response.status_code = 401
              mock_response.text = "Unauthorized"

              mock_post = AsyncMock(return_value=mock_response)
              mock_client.return_value.__aenter__.return_value.post = mock_post

              result = await telegram_channel.send(notification_target, notification_payload)

              assert result is False


      @pytest.mark.asyncio
      async def test_telegram_send_connection_error(telegram_channel, notification_target, notification_payload):
          """Test handling of connection error."""
          with patch("httpx.AsyncClient") as mock_client:
              mock_post = AsyncMock(side_effect=ConnectionError("Network error"))
              mock_client.return_value.__aenter__.return_value.post = mock_post

              result = await telegram_channel.send(notification_target, notification_payload)

              assert result is False
      ```

      Archivo: `tests/unit/alerting_system/test_telegram_helper.py`

      ```python
      import pytest
      from unittest.mock import AsyncMock, MagicMock, patch

      from app.services.alerting_system.telegram_helper import TelegramBotHelper


      @pytest.mark.asyncio
      async def test_test_bot_token_valid():
          """Test bot token validation with valid token."""
          with patch("httpx.AsyncClient") as mock_client:
              mock_response = MagicMock()
              mock_response.status_code = 200
              mock_response.json.return_value = {
                  "ok": True,
                  "result": {"id": 123, "username": "test_bot", "first_name": "Test Bot"},
              }

              mock_get = AsyncMock(return_value=mock_response)
              mock_client.return_value.__aenter__.return_value.get = mock_get

              result = await TelegramBotHelper.test_bot_token("test_token")

              assert result["valid"] is True
              assert result["bot_info"]["username"] == "test_bot"


      @pytest.mark.asyncio
      async def test_test_bot_token_invalid():
          """Test bot token validation with invalid token."""
          with patch("httpx.AsyncClient") as mock_client:
              mock_response = MagicMock()
              mock_response.status_code = 200
              mock_response.json.return_value = {
                  "ok": False,
                  "description": "Unauthorized",
              }

              mock_get = AsyncMock(return_value=mock_response)
              mock_client.return_value.__aenter__.return_value.get = mock_get

              result = await TelegramBotHelper.test_bot_token("invalid_token")

              assert result["valid"] is False
              assert "Unauthorized" in result["error"]


      @pytest.mark.asyncio
      async def test_get_chat_id():
          """Test getting chat_id from bot updates."""
          with patch("httpx.AsyncClient") as mock_client:
              mock_response = MagicMock()
              mock_response.status_code = 200
              mock_response.json.return_value = {
                  "ok": True,
                  "result": [
                      {
                          "message": {
                              "chat": {"id": 123456789},
                              "text": "/start",
                          }
                      }
                  ],
              }

              mock_get = AsyncMock(return_value=mock_response)
              mock_client.return_value.__aenter__.return_value.get = mock_get

              chat_id = await TelegramBotHelper.get_chat_id("test_token")

              assert chat_id == 123456789


      @pytest.mark.asyncio
      async def test_send_test_message():
          """Test sending a test message."""
          with patch("httpx.AsyncClient") as mock_client:
              mock_response = MagicMock()
              mock_response.status_code = 200
              mock_response.json.return_value = {"ok": True}

              mock_post = AsyncMock(return_value=mock_response)
              mock_client.return_value.__aenter__.return_value.post = mock_post

              result = await TelegramBotHelper.send_test_message("test_token", 123456789, "Test message")

              assert result is True
      ```

      ### PASO 7: Update __init__.py

      Archivo: `app/services/alerting_system/__init__.py`

      Update exports to include telegram helper:

      ```python
      from .notification_channels import NotificationDispatcher, TelegramChannel
      # ... other imports

      __all__ = [
          # ... other exports
          "TelegramChannel",
      ]
      ```

      ### PASO 8: Update UserSettings Integration

      The UserSettings already has telegram fields in NotificationSettings.
      We just need to make sure the alerting system can use them.

      Create a helper to convert UserSettings to NotificationTarget:

      Archivo: `app/services/alerting_system/user_config_adapter.py`

      ```python
      """
      Adapter to convert UserSettings to Alerting System configuration.
      """
      from typing import List

      from app.services.alerting_system.models import NotificationChannelType, NotificationTarget
      from app.user_config.user_settings import NotificationSettings, UserSettings


      def user_settings_to_notification_targets(settings: UserSettings) -> List[NotificationTarget]:
          """
          Convert UserSettings notification preferences to NotificationTarget list.

          Args:
              settings: UserSettings instance

          Returns:
              List of NotificationTarget objects
          """
          targets = []
          notifications = settings.notifications

          # Telegram
          if notifications.enable_telegram and notifications.telegram_chat_id:
              # Get bot_token from environment or secure storage
              import os

              bot_token = os.environ.get("TELEGRAM_BOT_TOKEN", "")

              targets.append(
                  NotificationTarget(
                      channel_type=NotificationChannelType.TELEGRAM,
                      endpoint=notifications.telegram_chat_id or "",
                      enabled=notifications.enable_telegram,
                      headers={
                          "telegram_bot_token": bot_token,
                          "telegram_chat_id": notifications.telegram_chat_id or "",
                      },
                  )
              )

          # Email
          if notifications.enable_email and notifications.email_address:
              # SMTP config from environment
              smtp_host = os.environ.get("SMTP_HOST", "localhost")
              smtp_port = os.environ.get("SMTP_PORT", "587")
              smtp_user = os.environ.get("SMTP_USER", "")
              smtp_password = os.environ.get("SMTP_PASSWORD", "")
              from_address = os.environ.get("SMTP_FROM", "alerts@algotrading.local")

              targets.append(
                  NotificationTarget(
                      channel_type=NotificationChannelType.EMAIL,
                      endpoint=notifications.email_address or "",
                      enabled=notifications.enable_email,
                      headers={
                          "smtp_host": smtp_host,
                          "smtp_port": smtp_port,
                          "smtp_user": smtp_user,
                          "smtp_password": smtp_password,
                          "smtp_use_tls": "true",
                          "from_address": from_address,
                      },
                  )
              )

          return targets
      ```

      ### PASO 9: Validate

      ```bash
      # Make script executable
      chmod +x scripts/setup_telegram.py

      # Validate syntax
      python -m py_compile app/services/alerting_system/telegram_helper.py
      python -m py_compile scripts/setup_telegram.py

      # Run tests
      pytest tests/unit/alerting_system/test_telegram_channel.py -v
      pytest tests/unit/alerting_system/test_telegram_helper.py -v

      # Test Telegram channel import
      python -c "from app.services.alerting_system.notification_channels import TelegramChannel; print('OK')"
      ```

      ### PASO 10: Update Checkpoint

      ```bash
      # Create checkpoint
      cat > .ralph/checkpoints/15_alerting_telegram_checkpoint.json << 'EOF'
      {
        "task": "15_alerting_telegram",
        "status": "COMPLETED",
        "completed_at": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
        "files_created": [
          "app/services/alerting_system/telegram_helper.py",
          "app/services/alerting_system/user_config_adapter.py",
          "scripts/setup_telegram.py",
          "tests/unit/alerting_system/test_telegram_channel.py",
          "tests/unit/alerting_system/test_telegram_helper.py"
        ],
        "files_modified": [
          "app/services/alerting_system/models.py",
          "app/services/alerting_system/notification_channels.py",
          "app/services/alerting_system/__init__.py"
        ],
        "features": [
          "TELEGRAM channel type added to NotificationChannelType",
          "TelegramChannel class implements NotificationChannel",
          "TelegramBotHelper for bot setup and testing",
          "setup_telegram.py CLI script for interactive bot setup",
          "Integration with UserSettings NotificationSettings",
          "Tests for TelegramChannel and TelegramBotHelper"
        ]
      }
      EOF

      # Update master checkpoint
      python .ralph/scripts/update_master_checkpoint.py 15_alerting_telegram COMPLETED
      ```

      ### PASO 11: Update Scratchpad

      Añadir a `.ralph/agent/scratchpad.md`:

      ```markdown
      ## 2026-02-09 XX:XX UTC - Task 15 COMPLETED

      ### Summary of Work Done

      **Task 15_alerting_telegram - COMPLETED**

      Implemented Telegram notification channel for alerting system:

      1. **TelegramChannel** - New notification channel type
         - Sends alerts via Telegram Bot API
         - Markdown formatting with emojis by severity
         - Configurable bot token and chat ID

      2. **TelegramBotHelper** - Bot setup and testing utilities
         - test_bot_token() - Validate bot token
         - get_chat_id() - Retrieve chat ID from updates
         - send_test_message() - Send test message
         - setup_telegram_bot_interactive() - Interactive setup

      3. **setup_telegram.py** - CLI script for interactive bot setup

      4. **user_config_adapter.py** - Adapter to convert UserSettings to NotificationTarget

      ### Integration Points
      - Added TELEGRAM to NotificationChannelType enum
      - Registered TelegramChannel in NotificationDispatcher
      - Integrated with UserSettings NotificationSettings
      - Uses environment variables for secure credential storage

      ### Validation Results
      - TelegramChannel implements NotificationChannel: PASSED
      - TelegramBotHelper tests pass: PASSED
      - Integration with existing alerting system: PASSED

      ### Next Task
      16_simple_dashboard (Phase 5 - fourth task of 4)

      ### Overall Progress
      - 15 out of 19 tasks COMPLETED
      - Phase 5 User Interface Layer: 3/4 IN PROGRESS
      ```

      ### Event Format:
      Completado: `ralph emit "telegram.complete" "task=15_alerting_telegram"`
      Bloqueado: `ralph emit "telegram.blocked" "error=..."`

  final_reviewer:
    name: "Telegram Reviewer"
    description: "Verifica Telegram implementation"
    triggers: ["telegram.complete"]
    publishes: ["telegram.final_summary"]
    instructions: |
      ## FINAL REVIEW

      ```bash
      # 1. Verificar archivos creados
      ls app/services/alerting_system/telegram_helper.py
      ls app/services/alerting_system/user_config_adapter.py
      ls scripts/setup_telegram.py
      ls tests/unit/alerting_system/test_telegram_channel.py
      ls tests/unit/alerting_system/test_telegram_helper.py

      # 2. Validar sintaxis
      python -m py_compile app/services/alerting_system/telegram_helper.py
      python -m py_compile scripts/setup_telegram.py

      # 3. Ejecutar tests
      pytest tests/unit/alerting_system/test_telegram_channel.py -v
      pytest tests/unit/alerting_system/test_telegram_helper.py -v

      # 4. Validar imports
      python -c "from app.services.alerting_system.notification_channels import TelegramChannel; print('OK')"
      python -c "from app.services.alerting_system.models import NotificationChannelType; assert NotificationChannelType.TELEGRAM; print('OK')"

      # 5. Verificar dispatcher registration
      python -c "from app.services.alerting_system.notification_channels import NotificationDispatcher; d = NotificationDispatcher(); assert NotificationChannelType.TELEGRAM in d.channels; print('OK')"
      ```

      Si TODO limpio: `ralph emit "telegram.final_summary" "all_passed"`

tasks:
  enabled: true

memories:
  enabled: true
  inject: auto
  budget: 2000

checkpoint:
  file: ".ralph/checkpoints/15_alerting_telegram_checkpoint.json"
