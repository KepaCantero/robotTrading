# Mean Reversion Strategy Configuration
name: mean_reversion
enabled: true
weight: 0.8

# Strategy-specific parameters
parameters:
  z_score_threshold: 1.0 # Increased from 0.5 to reduce false signals
  lookback_period: 20
  mean_reversion_speed: 0.1
  volatility_threshold: 0.02 # Stricter volatility filter (2% ATR floor)
  min_reversion_signal: 0.02
  max_reversion_signal: 0.15
  min_volume: 100000 # Minimum average volume filter
  atr_floor: 0.008 # ATR must be > 0.8% to trade

# Risk parameters
max_position_size: 0.08
stop_loss_pct: 0.04 # Increased from 3% to 4% (less frequent stops)
take_profit_pct: 0.12 # Increased from 6% to 12% (better risk/reward ratio 1:3)
max_trades_per_day: 2 # Limit overtrading
max_trades_per_week: 10

# Performance thresholds
min_sharpe_ratio: 1.0
max_drawdown: 0.15
min_win_rate: 0.4
